-- ==================================================
-- FOXXIEHUB ULTIMATE [FUTURISTIC EDITION]
-- ==================================================

print("Loading FoxxieHub Ultimate (Cybernetic Edition)...")

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
if not player then
    repeat task.wait() until Players.LocalPlayer
    player = Players.LocalPlayer
end

repeat task.wait() until player and player.Character

-- ==================================================
-- ADVANCED ANTI-CHEAT BYPASS HOOKS
-- ==================================================
task.spawn(function()
    pcall(function()
        local mt = getrawmetatable(game)
        setreadonly(mt, false)
        local oldNamecall = mt.__namecall
        
        mt.__namecall = newcclosure(function(self, ...)
            local method = getnamecallmethod()
            local args = {...}
            
            if method == "Kick" or method == "kick" then
                if self == player then
                    return
                end
            end
            
            return oldNamecall(self, ...)
        end)
        setreadonly(mt, true)
    end)
    
    RunService.Stepped:Connect(function()
        pcall(function()
            if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
                hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
            end
        end)
    end)
end)

-- ==================================================
-- CYBERNETIC TERMINAL LOADING SCREEN
-- ==================================================

local loadGui = Instance.new("ScreenGui")
loadGui.Name = "FoxxieHubLoader"
loadGui.ResetOnSpawn = false
loadGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
loadGui.Parent = player:WaitForChild("PlayerGui")

local termFrame = Instance.new("Frame")
termFrame.Size = UDim2.new(0, 400, 0, 260)
termFrame.Position = UDim2.new(0.5, -200, 0.5, -130)
termFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 12)
termFrame.BorderSizePixel = 0
termFrame.Parent = loadGui

local termCorner = Instance.new("UICorner")
termCorner.CornerRadius = UDim.new(0, 4)
termCorner.Parent = termFrame

local termStroke = Instance.new("UIStroke")
termStroke.Color = Color3.fromRGB(0, 255, 180)
termStroke.Thickness = 2
termStroke.Parent = termFrame

local termHeader = Instance.new("Frame")
termHeader.Size = UDim2.new(1, 0, 0, 30)
termHeader.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
termHeader.BorderSizePixel = 0
termHeader.Parent = termFrame

local termTitle = Instance.new("TextLabel")
termTitle.Size = UDim2.new(1, -16, 1, 0)
termTitle.Position = UDim2.new(0, 12, 0, 0)
termTitle.BackgroundTransparency = 1
termTitle.TextColor3 = Color3.fromRGB(0, 255, 180)
termTitle.TextSize = 12
termTitle.Font = Enum.Font.Code
termTitle.TextXAlignment = Enum.TextXAlignment.Left
termTitle.Text = "SYS_INIT // foxxie_core_v4.exe"
termTitle.Parent = termHeader

local termText = Instance.new("TextLabel")
termText.Size = UDim2.new(1, -24, 1, -44)
termText.Position = UDim2.new(0, 12, 0, 38)
termText.BackgroundTransparency = 1
termText.TextColor3 = Color3.fromRGB(100, 255, 200)
termText.TextSize = 11
termText.Font = Enum.Font.Code
termText.TextXAlignment = Enum.TextXAlignment.Left
termText.TextYAlignment = Enum.TextYAlignment.Top
termText.Text = "[ * ] Bypassing quantum firewalls...\n[ * ] Initializing holographic render matrix...\n[OK] User signature verified: aliiiwyddd\n[OK] Loading neural bypass hooks..."
termText.Parent = termFrame

task.wait(1.0)
termText.Text = termText.Text .. "\n[OK] Synchronizing vector graphics...\n[OK] Launching cyber interface..."
task.wait(1.2)
loadGui:Destroy()

-- ==================================================
-- CYBERPUNK AUTHENTICATION KEY SYSTEM
-- ==================================================

local keyGui = Instance.new("ScreenGui")
keyGui.Name = "FoxxieHubKey"
keyGui.ResetOnSpawn = false
keyGui.Parent = player.PlayerGui

local keyOverlay = Instance.new("Frame")
keyOverlay.Size = UDim2.new(1, 0, 1, 0)
keyOverlay.BackgroundColor3 = Color3.fromRGB(3, 3, 5)
keyOverlay.BackgroundTransparency = 0.6
keyOverlay.BorderSizePixel = 0
keyOverlay.Parent = keyGui

local keyFrame = Instance.new("Frame")
keyFrame.Size = UDim2.new(0, 380, 0, 230)
keyFrame.Position = UDim2.new(0.5, -190, 0.5, -115)
keyFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 16)
keyFrame.BorderSizePixel = 0
keyFrame.Parent = keyGui

local keyCorner = Instance.new("UICorner")
keyCorner.CornerRadius = UDim.new(0, 6)
keyCorner.Parent = keyFrame

local keyStroke = Instance.new("UIStroke")
keyStroke.Color = Color3.fromRGB(160, 50, 255)
keyStroke.Thickness = 2
keyStroke.Parent = keyFrame

local keyHeader = Instance.new("TextLabel")
keyHeader.Size = UDim2.new(1, 0, 0, 45)
keyHeader.BackgroundTransparency = 1
keyHeader.TextColor3 = Color3.fromRGB(240, 240, 255)
keyHeader.TextSize = 14
keyHeader.Font = Enum.Font.SciFi
keyHeader.Text = "⚡ NEURAL AUTHENTICATION"
keyHeader.Parent = keyFrame

local keySub = Instance.new("TextLabel")
keySub.Size = UDim2.new(1, -30, 0, 25)
keySub.Position = UDim2.new(0, 15, 0, 42)
keySub.BackgroundTransparency = 1
keySub.TextColor3 = Color3.fromRGB(130, 130, 160)
keySub.TextSize = 10
keySub.Font = Enum.Font.Code
keySub.Text = "Enter security clearance key. TikTok (.vfsv)"
keySub.Parent = keyFrame

local keyBoxContainer = Instance.new("Frame")
keyBoxContainer.Size = UDim2.new(1, -40, 0, 40)
keyBoxContainer.Position = UDim2.new(0, 20, 0, 85)
keyBoxContainer.BackgroundColor3 = Color3.fromRGB(16, 16, 26)
keyBoxContainer.BorderSizePixel = 0
keyBoxContainer.Parent = keyFrame

local keyBoxCorner = Instance.new("UICorner")
keyBoxCorner.CornerRadius = UDim.new(0, 4)
keyBoxCorner.Parent = keyBoxContainer

local keyBoxStroke = Instance.new("UIStroke")
keyBoxStroke.Color = Color3.fromRGB(60, 60, 90)
keyBoxStroke.Thickness = 1
keyBoxStroke.Parent = keyBoxContainer

local keyBox = Instance.new("TextBox")
keyBox.Size = UDim2.new(1, -20, 1, 0)
keyBox.Position = UDim2.new(0, 10, 0, 0)
keyBox.BackgroundTransparency = 1
keyBox.TextColor3 = Color3.fromRGB(0, 255, 180)
keyBox.PlaceholderText = "INPUT_KEY (Ali)..."
keyBox.PlaceholderColor3 = Color3.fromRGB(70, 70, 100)
keyBox.Text = ""
keyBox.TextSize = 12
keyBox.Font = Enum.Font.Code
keyBox.Parent = keyBoxContainer

local submitBtn = Instance.new("TextButton")
submitBtn.Size = UDim2.new(1, -40, 0, 42)
submitBtn.Position = UDim2.new(0, 20, 0, 145)
submitBtn.BackgroundColor3 = Color3.fromRGB(160, 50, 255)
submitBtn.BorderSizePixel = 0
submitBtn.Text = "INITIALIZE ACCESS"
submitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
submitBtn.TextSize = 12
submitBtn.Font = Enum.Font.SciFi
submitBtn.Parent = keyFrame

local submitCorner = Instance.new("UICorner")
submitCorner.CornerRadius = UDim.new(0, 4)
submitCorner.Parent = submitBtn

local authenticated = false
submitBtn.MouseButton1Click:Connect(function()
    if keyBox.Text == "Ali" then
        authenticated = true
        TweenService:Create(keyFrame, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), Position = keyFrame.Position + UDim2.new(0, 190, 0, 115)}):Play()
        TweenService:Create(keyOverlay, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
        task.wait(0.3)
        keyGui:Destroy()
    else
        submitBtn.Text = "ACCESS DENIED"
        submitBtn.BackgroundColor3 = Color3.fromRGB(255, 40, 80)
        task.wait(1)
        submitBtn.Text = "INITIALIZE ACCESS"
        submitBtn.BackgroundColor3 = Color3.fromRGB(160, 50, 255)
    end
end)

repeat task.wait() until authenticated

-- ==================================================
-- COLOR PARSER & RAINBOW ENGINE
-- ==================================================

local rainbowTick = 0
RunService.RenderStepped:Connect(function(dt)
    rainbowTick = rainbowTick + dt
end)

local function parseColor(input)
    if typeof(input) == "Color3" then return input end
    local str = string.lower(tostring(input or "purple"))
    
    if str == "rainbow" then
        local hue = (rainbowTick % 5) / 5
        return Color3.fromHSV(hue, 1, 1)
    end
    
    local presets = {
        red = Color3.fromRGB(255, 50, 50),
        green = Color3.fromRGB(50, 255, 120),
        blue = Color3.fromRGB(50, 140, 255),
        purple = Color3.fromRGB(160, 50, 255),
        pink = Color3.fromRGB(255, 80, 180),
        yellow = Color3.fromRGB(255, 220, 0),
        orange = Color3.fromRGB(255, 130, 0),
        cyan = Color3.fromRGB(0, 240, 255),
        white = Color3.fromRGB(255, 255, 255),
        black = Color3.fromRGB(10, 10, 15),
        gray = Color3.fromRGB(120, 120, 140),
        lime = Color3.fromRGB(40, 255, 40),
        crimson = Color3.fromRGB(200, 10, 50),
        indigo = Color3.fromRGB(60, 0, 150),
        teal = Color3.fromRGB(0, 150, 150),
        gold = Color3.fromRGB(255, 200, 0)
    }
    
    if presets[str] then return presets[str] end
    
    if string.find(str, ",") then
        local parts = string.split(str, ",")
        local r, g, b = tonumber(parts[1]), tonumber(parts[2]), tonumber(parts[3])
        if r and g and b then
            return Color3.fromRGB(r, g, b)
        end
    end
    
    return Color3.fromRGB(160, 50, 255)
end

-- ==================================================
-- CONFIGURATION SETTINGS
-- ==================================================

local config = {
    enabled = false,
    showFov = true,
    smoothness = 0.45,
    prediction = 0.50,
    fovRadius = 110,
    maxDistance = 250,
    targetPart = "HumanoidRootPart",
    keybind = Enum.KeyCode.Q,
    camlockMovable = true,
    
    espEnabled = false,
    chamsEnabled = false,
    boxEnabled = true,
    nameEnabled = true,
    healthEnabled = true,
    metersEnabled = true,
    traceEnabled = true,
    
    themeColorName = "purple",
    boxColorName = "white",
    espColorName = "purple",
    traceColorName = "white",
    
    speedEnabled = false,
    speedValue = 32,
    jumpEnabled = false,
    jumpValue = 100,
    
    currentFont = Enum.Font.Code,
    
    grassEnabled = true,
    fogEnabled = true,
    shadowsEnabled = true,
    
    decalId = ""
}

print("✅ Cybernetic configuration compiled.")

-- ==================================================
-- MAIN HOLOGRAPHIC GUI SETUP
-- ==================================================

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FoxxieHubUltimate"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = player.PlayerGui

-- Main Container Window
local container = Instance.new("Frame")
container.Size = UDim2.new(0, 330, 0, 480)
container.Position = UDim2.new(0.5, -165, 0.5, -240)
container.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
container.BackgroundTransparency = 0.08
container.BorderSizePixel = 0
container.ClipsDescendants = false
container.Parent = screenGui

local containerCorner = Instance.new("UICorner")
containerCorner.CornerRadius = UDim.new(0, 6)
containerCorner.Parent = container

local containerStroke = Instance.new("UIStroke")
containerStroke.Color = parseColor(config.themeColorName)
containerStroke.Thickness = 1.8
containerStroke.Parent = container

-- Custom Background Image
local bgImageLabel = Instance.new("ImageLabel")
bgImageLabel.Size = UDim2.new(1, 0, 1, 0)
bgImageLabel.BackgroundTransparency = 1
bgImageLabel.ScaleType = Enum.ScaleType.Slice
bgImageLabel.Visible = false
bgImageLabel.ZIndex = 0
bgImageLabel.Parent = container

-- Draggable implementation
local dragging, dragInput, dragStart, startPos
container.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = container.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

RunService.RenderStepped:Connect(function()
    if dragging and dragInput then
        local delta = dragInput.Position - dragStart
        container.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Header Bar
local header = Instance.new("Frame")
header.Size = UDim2.new(1, 0, 0, 40)
header.BackgroundColor3 = Color3.fromRGB(16, 16, 24)
header.BorderSizePixel = 0
header.ZIndex = 2
header.Parent = container

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 6)
headerCorner.Parent = header

local headerFix = Instance.new("Frame")
headerFix.Size = UDim2.new(1, 0, 0, 10)
headerFix.Position = UDim2.new(0, 0, 1, -10)
headerFix.BackgroundColor3 = Color3.fromRGB(16, 16, 24)
headerFix.BorderSizePixel = 0
headerFix.ZIndex = 2
headerFix.Parent = header

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -16, 1, 0)
title.Position = UDim2.new(0, 12, 0, 0)
title.BackgroundTransparency = 1
title.Text = "FOXXIE.SYS // aliiiwyddd"
title.TextColor3 = Color3.fromRGB(240, 240, 255)
title.TextSize = 11
title.Font = Enum.Font.SciFi
title.ZIndex = 3
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = header

-- ==================================================
-- HOLOGRAPHIC TAB SYSTEM
-- ==================================================

local tabButtonsFrame = Instance.new("Frame")
tabButtonsFrame.Size = UDim2.new(1, -16, 0, 36)
tabButtonsFrame.Position = UDim2.new(0, 8, 0, 46)
tabButtonsFrame.BackgroundColor3 = Color3.fromRGB(7, 7, 11)
tabButtonsFrame.BorderSizePixel = 0
tabButtonsFrame.ZIndex = 2
tabButtonsFrame.Parent = container

local tabButtonsCorner = Instance.new("UICorner")
tabButtonsCorner.CornerRadius = UDim.new(0, 4)
tabButtonsCorner.Parent = tabButtonsFrame

local tabContentFrame = Instance.new("Frame")
tabContentFrame.Size = UDim2.new(1, 0, 1, -90)
tabContentFrame.Position = UDim2.new(0, 0, 0, 90)
tabContentFrame.BackgroundTransparency = 1
tabContentFrame.ZIndex = 2
tabContentFrame.Parent = container

local tabs = {}

local function createTab(name)
    local tabScroll = Instance.new("ScrollingFrame")
    tabScroll.Size = UDim2.new(1, -16, 1, -10)
    tabScroll.Position = UDim2.new(0, 8, 0, 5)
    tabScroll.BackgroundTransparency = 1
    tabScroll.BorderSizePixel = 0
    tabScroll.CanvasSize = UDim2.new(0, 0, 0, 700)
    tabScroll.ScrollBarThickness = 2
    tabScroll.ScrollBarImageColor3 = parseColor(config.themeColorName)
    tabScroll.Visible = false
    tabScroll.ZIndex = 2
    tabScroll.Parent = tabContentFrame
    
    local tabBtn = Instance.new("TextButton")
    tabBtn.Size = UDim2.new(0, 50, 1, 0)
    tabBtn.BackgroundColor3 = Color3.fromRGB(7, 7, 11)
    tabBtn.BackgroundTransparency = 1
    tabBtn.BorderSizePixel = 0
    tabBtn.Text = name
    tabBtn.TextColor3 = Color3.fromRGB(120, 120, 150)
    tabBtn.TextSize = 10
    tabBtn.Font = Enum.Font.Code
    tabBtn.ZIndex = 3
    tabBtn.Parent = tabButtonsFrame
    
    local tabBtnCorner = Instance.new("UICorner")
    tabBtnCorner.CornerRadius = UDim.new(0, 4)
    tabBtnCorner.Parent = tabBtn
    
    tabBtn.MouseButton1Click:Connect(function()
        for _, t in pairs(tabs) do
            t.scroll.Visible = false
            t.btn.TextColor3 = Color3.fromRGB(120, 120, 150)
            TweenService:Create(t.btn, TweenInfo.new(0.2), {BackgroundTransparency = 1}):Play()
        end
        tabScroll.Visible = true
        tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        TweenService:Create(tabBtn, TweenInfo.new(0.2), {BackgroundTransparency = 0, BackgroundColor3 = parseColor(config.themeColorName)}):Play()
    end)
    
    table.insert(tabs, {scroll = tabScroll, btn = tabBtn})
    
    if #tabs == 1 then
        tabScroll.Visible = true
        tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        tabBtn.BackgroundTransparency = 0
        tabBtn.BackgroundColor3 = parseColor(config.themeColorName)
    end
    
    return tabScroll
end

task.defer(function()
    local count = #tabs
    local width = 1 / count
    for i, t in ipairs(tabs) do
        t.btn.Size = UDim2.new(width, -4, 1, -4)
        t.btn.Position = UDim2.new((i - 1) * width, 2, 0, 2)
    end
end)

local tabCamlock = createTab("MAIN")
local tabEsp = createTab("ESP")
local tabStats = createTab("STATS")
local tabColor = createTab("COLORS")
local tabFps = createTab("FPS")

-- ==================================================
-- COLOR PICKER MATRIX POPUP
-- ==================================================

local colorPickerPopup = Instance.new("Frame")
colorPickerPopup.Size = UDim2.new(0, 300, 0, 260)
colorPickerPopup.Position = UDim2.new(0.5, -150, 0.5, -130)
colorPickerPopup.BackgroundColor3 = Color3.fromRGB(10, 10, 16)
colorPickerPopup.BorderSizePixel = 0
colorPickerPopup.Visible = false
colorPickerPopup.ZIndex = 15
colorPickerPopup.Parent = screenGui

local popupCorner = Instance.new("UICorner")
popupCorner.CornerRadius = UDim.new(0, 6)
popupCorner.Parent = colorPickerPopup

local popupStroke = Instance.new("UIStroke")
popupStroke.Color = Color3.fromRGB(160, 50, 255)
popupStroke.Thickness = 1.8
popupStroke.Parent = colorPickerPopup

local popupTitle = Instance.new("TextLabel")
popupTitle.Size = UDim2.new(1, 0, 0, 36)
popupTitle.BackgroundTransparency = 1
popupTitle.Text = "SELECT NEURAL SPECTRUM"
popupTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
popupTitle.TextSize = 11
popupTitle.Font = Enum.Font.SciFi
popupTitle.ZIndex = 16
popupTitle.Parent = colorPickerPopup

local colorGridContainer = Instance.new("ScrollingFrame")
colorGridContainer.Size = UDim2.new(1, -24, 1, -46)
colorGridContainer.Position = UDim2.new(0, 12, 0, 38)
colorGridContainer.BackgroundTransparency = 1
colorGridContainer.CanvasSize = UDim2.new(0, 0, 0, 160)
colorGridContainer.ScrollBarThickness = 2
colorGridContainer.ZIndex = 16
colorGridContainer.Parent = colorPickerPopup

local colorGrid = Instance.new("UIGridLayout")
colorGrid.CellSize = UDim2.new(0, 78, 0, 30)
colorGrid.CellPadding = UDim2.new(0, 8, 0, 8)
colorGrid.Parent = colorGridContainer

local colorsList = {"red", "green", "blue", "purple", "pink", "yellow", "orange", "cyan", "white", "black", "gray", "lime", "crimson", "indigo", "teal", "gold", "rainbow"}
local activeColorCallback = nil

for _, colName in ipairs(colorsList) do
    local btn = Instance.new("TextButton")
    btn.BackgroundColor3 = parseColor(colName)
    btn.BorderSizePixel = 0
    btn.Text = string.upper(colName)
    btn.TextColor3 = (colName == "white" or colName == "yellow" or colName == "cyan" or colName == "lime" or colName == "gold") and Color3.fromRGB(10, 10, 10) or Color3.fromRGB(255, 255, 255)
    btn.TextSize = 10
    btn.Font = Enum.Font.Code
    btn.ZIndex = 17
    btn.Parent = colorGridContainer
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 4)
    btnCorner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        if activeColorCallback then
            activeColorCallback(colName)
        end
        TweenService:Create(colorPickerPopup, TweenInfo.new(0.2, Enum.EasingStyle.Exponential, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 0, 0, 0),
            Position = colorPickerPopup.Position + UDim2.new(0, 150, 0, 130)
        }):Play()
        task.wait(0.2)
        colorPickerPopup.Visible = false
    end)
end

local function showColorPicker(callback)
    activeColorCallback = callback
    colorPickerPopup.Visible = true
    colorPickerPopup.Size = UDim2.new(0, 0, 0, 0)
    colorPickerPopup.Position = UDim2.new(0.5, 0, 0.5, 0)
    
    TweenService:Create(colorPickerPopup, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 300, 0, 260),
        Position = UDim2.new(0.5, -150, 0.5, -130)
    }):Play()
end

-- ==================================================
-- FUTURISTIC COMPONENT BUILDERS
-- ==================================================

local function addToggle(parentScroll, name, defaultState, callback)
    local yPos = #parentScroll:GetChildren() * 42
    parentScroll.CanvasSize = UDim2.new(0, 0, 0, yPos + 46)
    
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 36)
    row.Position = UDim2.new(0, 0, 0, yPos)
    row.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
    row.BorderSizePixel = 0
    row.ZIndex = 2
    row.Parent = parentScroll
    
    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 4)
    rowCorner.Parent = row
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.7, 0, 1, 0)
    lbl.Position = UDim2.new(0, 12, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = string.upper(name)
    lbl.TextColor3 = Color3.fromRGB(220, 220, 240)
    lbl.TextSize = 10
    lbl.Font = config.currentFont
    lbl.ZIndex = 2
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = row
    
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0, 40, 0, 18)
    toggleBtn.Position = UDim2.new(1, -48, 0.5, -9)
    toggleBtn.BackgroundColor3 = defaultState and parseColor(config.themeColorName) or Color3.fromRGB(25, 25, 38)
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Text = ""
    toggleBtn.ZIndex = 2
    toggleBtn.Parent = row
    
    local toggleBtnCorner = Instance.new("UICorner")
    toggleBtnCorner.CornerRadius = UDim.new(1, 0)
    toggleBtnCorner.Parent = toggleBtn
    
    local toggleCircle = Instance.new("Frame")
    toggleCircle.Size = UDim2.new(0, 14, 0, 14)
    toggleCircle.Position = defaultState and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)
    toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    toggleCircle.BorderSizePixel = 0
    toggleCircle.ZIndex = 2
    toggleCircle.Parent = toggleBtn
    
    local circleCorner = Instance.new("UICorner")
    circleCorner.CornerRadius = UDim.new(1, 0)
    circleCorner.Parent = toggleCircle
    
    local state = defaultState
    toggleBtn.MouseButton1Click:Connect(function()
        state = not state
        callback(state)
        TweenService:Create(toggleBtn, TweenInfo.new(0.2), {BackgroundColor3 = state and parseColor(config.themeColorName) or Color3.fromRGB(25, 25, 38)}):Play()
        TweenService:Create(toggleCircle, TweenInfo.new(0.2), {Position = state and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)}):Play()
    end)
end

local function addSlider(parentScroll, name, min, max, defaultVal, callback)
    local yPos = #parentScroll:GetChildren() * 42
    parentScroll.CanvasSize = UDim2.new(0, 0, 0, yPos + 56)
    
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 46)
    row.Position = UDim2.new(0, 0, 0, yPos)
    row.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
    row.BorderSizePixel = 0
    row.ZIndex = 2
    row.Parent = parentScroll
    
    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 4)
    rowCorner.Parent = row
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.7, 0, 0, 20)
    lbl.Position = UDim2.new(0, 12, 0, 4)
    lbl.BackgroundTransparency = 1
    lbl.Text = string.upper(name)
    lbl.TextColor3 = Color3.fromRGB(220, 220, 240)
    lbl.TextSize = 10
    lbl.Font = config.currentFont
    lbl.ZIndex = 2
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = row
    
    local valLbl = Instance.new("TextLabel")
    valLbl.Size = UDim2.new(0.3, 0, 0, 20)
    valLbl.Position = UDim2.new(1, -12, 0, 4)
    valLbl.BackgroundTransparency = 1
    valLbl.Text = tostring(defaultVal)
    valLbl.TextColor3 = parseColor(config.themeColorName)
    valLbl.TextSize = 10
    valLbl.Font = config.currentFont
    valLbl.ZIndex = 2
    valLbl.TextXAlignment = Enum.TextXAlignment.Right
    valLbl.Parent = row
    
    local sliderBar = Instance.new("Frame")
    sliderBar.Size = UDim2.new(1, -24, 0, 5)
    sliderBar.Position = UDim2.new(0, 12, 0, 30)
    sliderBar.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
    sliderBar.BorderSizePixel = 0
    sliderBar.ZIndex = 2
    sliderBar.Parent = row
    
    local sliderBarCorner = Instance.new("UICorner")
    sliderBarCorner.CornerRadius = UDim.new(1, 0)
    sliderBarCorner.Parent = sliderBar
    
    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((defaultVal - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = parseColor(config.themeColorName)
    fill.BorderSizePixel = 0
    fill.ZIndex = 2
    fill.Parent = sliderBar
    
    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(1, 0)
    fillCorner.Parent = fill
    
    local draggingSlider = false
    sliderBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingSlider = true
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingSlider = false
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if draggingSlider and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local pos = math.clamp((input.Position.X - sliderBar.AbsolutePosition.X) / sliderBar.AbsoluteSize.X, 0, 1)
            fill.Size = UDim2.new(pos, 0, 1, 0)
            local currentVal = math.round((min + (max - min) * pos) * 100) / 100
            valLbl.Text = tostring(currentVal)
            callback(currentVal)
        end
    end)
end

local function addDropdown(parentScroll, name, options, defaultOption, callback)
    local yPos = #parentScroll:GetChildren() * 42
    parentScroll.CanvasSize = UDim2.new(0, 0, 0, yPos + 46)
    
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 36)
    row.Position = UDim2.new(0, 0, 0, yPos)
    row.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
    row.BorderSizePixel = 0
    row.ZIndex = 2
    row.Parent = parentScroll
    
    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 4)
    rowCorner.Parent = row
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.5, 0, 1, 0)
    lbl.Position = UDim2.new(0, 12, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = string.upper(name)
    lbl.TextColor3 = Color3.fromRGB(220, 220, 240)
    lbl.TextSize = 10
    lbl.Font = config.currentFont
    lbl.ZIndex = 2
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = row
    
    local dropBtn = Instance.new("TextButton")
    dropBtn.Size = UDim2.new(0, 130, 0, 24)
    dropBtn.Position = UDim2.new(1, -142, 0.5, -12)
    dropBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 34)
    dropBtn.BorderSizePixel = 0
    dropBtn.Text = defaultOption
    dropBtn.TextColor3 = Color3.fromRGB(0, 255, 180)
    dropBtn.TextSize = 10
    dropBtn.Font = config.currentFont
    dropBtn.ZIndex = 2
    dropBtn.Parent = row
    
    local dropBtnCorner = Instance.new("UICorner")
    dropBtnCorner.CornerRadius = UDim.new(0, 4)
    dropBtnCorner.Parent = dropBtn
    
    local currentIdx = 1
    for i, opt in ipairs(options) do
        if opt == defaultOption then
            currentIdx = i
            break
        end
    end
    
    dropBtn.MouseButton1Click:Connect(function()
        currentIdx = currentIdx + 1
        if currentIdx > #options then
            currentIdx = 1
        end
        local chosen = options[currentIdx]
        dropBtn.Text = chosen
        callback(chosen)
    end)
end

local function addColorBox(parentScroll, name, defaultText, callback)
    local yPos = #parentScroll:GetChildren() * 42
    parentScroll.CanvasSize = UDim2.new(0, 0, 0, yPos + 46)
    
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 36)
    row.Position = UDim2.new(0, 0, 0, yPos)
    row.BackgroundColor3 = Color3.fromRGB(14, 14, 22)
    row.BorderSizePixel = 0
    row.ZIndex = 2
    row.Parent = parentScroll
    
    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 4)
    rowCorner.Parent = row
    
    local lbl = Instance.new("TextButton")
    lbl.Size = UDim2.new(0.55, 0, 1, 0)
    lbl.Position = UDim2.new(0, 12, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = string.upper(name) .. " [SYS]"
    lbl.TextColor3 = Color3.fromRGB(220, 220, 240)
    lbl.TextSize = 10
    lbl.Font = config.currentFont
    lbl.ZIndex = 2
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = row
    
    local box = Instance.new("TextBox")
    box.Size = UDim2.new(0, 110, 0, 24)
    box.Position = UDim2.new(1, -122, 0.5, -12)
    box.BackgroundColor3 = Color3.fromRGB(22, 22, 34)
    box.BorderSizePixel = 0
    box.Text = defaultText
    box.TextColor3 = Color3.fromRGB(0, 255, 180)
    box.TextSize = 10
    box.Font = config.currentFont
    box.ZIndex = 2
    box.Parent = row
    
    local boxCorner = Instance.new("UICorner")
    boxCorner.CornerRadius = UDim.new(0, 4)
    boxCorner.Parent = box
    
    box.FocusLost:Connect(function()
        callback(box.Text)
    end)
    
    lbl.MouseButton1Click:Connect(function()
        showColorPicker(function(selectedColor)
            box.Text = selectedColor
            callback(selectedColor)
        end)
    end)
end

-- ==================================================
-- TAB CONTROLS POPULATION
-- ==================================================

addToggle(tabCamlock, "camlock enabled", config.enabled, function(state) config.enabled = state end)
addToggle(tabCamlock, "camlock movable", config.camlockMovable, function(state) config.camlockMovable = state end)
addToggle(tabCamlock, "silent aim circle", config.showFov, function(state) config.showFov = state end)
addDropdown(tabCamlock, "target part", {"HumanoidRootPart", "Head", "UpperTorso", "LowerTorso"}, config.targetPart, function(val) config.targetPart = val end)
addSlider(tabCamlock, "smoothness", 0.01, 1, config.smoothness, function(val) config.smoothness = val end)
addSlider(tabCamlock, "prediction", 0, 2, config.prediction, function(val) config.prediction = val end)
addSlider(tabCamlock, "circle radius", 20, 400, config.fovRadius, function(val) config.fovRadius = val end)

addToggle(tabEsp, "esp enabled", config.espEnabled, function(state) config.espEnabled = state end)
addToggle(tabEsp, "box 4x4", config.boxEnabled, function(state) config.boxEnabled = state end)
addToggle(tabEsp, "player names", config.nameEnabled, function(state) config.nameEnabled = state end)
addToggle(tabEsp, "health bar", config.healthEnabled, function(state) config.healthEnabled = state end)
addToggle(tabEsp, "meters away", config.metersEnabled, function(state) config.metersEnabled = state end)
addToggle(tabEsp, "trace lines", config.traceEnabled, function(state) config.traceEnabled = state end)
addToggle(tabEsp, "chams selector", config.chamsEnabled, function(state) config.chamsEnabled = state end)

addToggle(tabStats, "speed enabled", config.speedEnabled, function(state) config.speedEnabled = state end)
addSlider(tabStats, "speed value", 16, 200, config.speedValue, function(val) config.speedValue = val end)
addToggle(tabStats, "jump enabled", config.jumpEnabled, function(state) config.jumpEnabled = state end)
addSlider(tabStats, "jump value", 50, 300, config.jumpValue, function(val) config.jumpValue = val end)

addColorBox(tabColor, "theme color", config.themeColorName, function(txt) config.themeColorName = txt containerStroke.Color = parseColor(txt) end)
addColorBox(tabColor, "box color", config.boxColorName, function(txt) config.boxColorName = txt end)
addColorBox(tabColor, "esp color", config.espColorName, function(txt) config.espColorName = txt end)
addColorBox(tabColor, "trace color", config.traceColorName, function(txt) config.traceColorName = txt end)

-- Global Theme Override Button
local globalColorRow = Instance.new("Frame")
globalColorRow.Size = UDim2.new(1, 0, 0, 42)
globalColorRow.Position = UDim2.new(0, 0, 0, #tabColor:GetChildren() * 42)
globalColorRow.BackgroundTransparency = 1
globalColorRow.ZIndex = 2
globalColorRow.Parent = tabColor

local globalColorBtn = Instance.new("TextButton")
globalColorBtn.Size = UDim2.new(1, 0, 0, 34)
globalColorBtn.Position = UDim2.new(0, 0, 0, 0)
globalColorBtn.BackgroundColor3 = Color3.fromRGB(160, 50, 255)
globalColorBtn.BorderSizePixel = 0
globalColorBtn.Text = "OVERRIDE GLOBAL SPECTRUM"
globalColorBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
globalColorBtn.TextSize = 10
globalColorBtn.Font = Enum.Font.SciFi
globalColorBtn.ZIndex = 2
globalColorBtn.Parent = globalColorRow

local globalBtnCorner = Instance.new("UICorner")
globalBtnCorner.CornerRadius = UDim.new(0, 4)
globalBtnCorner.Parent = globalColorBtn

globalColorBtn.MouseButton1Click:Connect(function()
    showColorPicker(function(selectedColor)
        config.themeColorName = selectedColor
        config.boxColorName = selectedColor
        config.espColorName = selectedColor
        config.traceColorName = selectedColor
        containerStroke.Color = parseColor(selectedColor)
    end)
end)

-- Custom Decal ID Background
local bgAssetRow = Instance.new("Frame")
bgAssetRow.Size = UDim2.new(1, 0, 0, 48)
bgAssetRow.Position = UDim2.new(0, 0, 0, (#tabColor:GetChildren() * 42) + 6)
bgAssetRow.BackgroundTransparency = 1
bgAssetRow.ZIndex = 2
bgAssetRow.Parent = tabColor

local bgLbl = Instance.new("TextLabel")
bgLbl.Size = UDim2.new(1, 0, 0, 18)
bgLbl.Position = UDim2.new(0, 4, 0, 0)
bgLbl.BackgroundTransparency = 1
bgLbl.Text = "HOLOGRAPHIC DECAL OVERLAY:"
bgLbl.TextColor3 = Color3.fromRGB(220, 220, 240)
bgLbl.TextSize = 9
bgLbl.Font = Enum.Font.Code
bgLbl.ZIndex = 2
bgLbl.TextXAlignment = Enum.TextXAlignment.Left
bgLbl.Parent = bgAssetRow

local bgBox = Instance.new("TextBox")
bgBox.Size = UDim2.new(1, 0, 0, 26)
bgBox.Position = UDim2.new(0, 0, 0, 20)
bgBox.BackgroundColor3 = Color3.fromRGB(22, 22, 34)
bgBox.BorderSizePixel = 0
bgBox.PlaceholderText = "rbxassetid://..."
bgBox.Text = ""
bgBox.TextColor3 = Color3.fromRGB(0, 255, 180)
bgBox.TextSize = 10
bgBox.Font = Enum.Font.Code
bgBox.ZIndex = 2
bgBox.Parent = bgAssetRow

local bgBoxCorner = Instance.new("UICorner")
bgBoxCorner.CornerRadius = UDim.new(0, 4)
bgBoxCorner.Parent = bgBox

bgBox.FocusLost:Connect(function()
    config.decalId = bgBox.Text
    if config.decalId ~= "" then
        bgImageLabel.Image = config.decalId
        bgImageLabel.Visible = true
    else
        bgImageLabel.Visible = false
    end
end)

addToggle(tabFps, "remove grass", false, function(state)
    config.grassEnabled = not state
    pcall(function()
        for _, v in pairs(Workspace:GetDescendants()) do
            if v:IsA("Terrain") then
                v.WaterWaveSize = 0
            elseif v:IsA("BasePart") and (string.find(string.lower(v.Name), "grass") or string.find(string.lower(v.Name), "leaf")) then
                v.Transparency = state and 1 or 0
            end
        end
    end)
end)

addToggle(tabFps, "remove fog", false, function(state)
    config.fogEnabled = not state
    Lighting.FogEnd = state and 100000 or 1000
end)

addToggle(tabFps, "remove shadows", false, function(state)
    config.shadowsEnabled = not state
    Lighting.GlobalShadows = not state
end)

-- ==================================================
-- FLOATING HOLOGRAPHIC CONTROLS
-- ==================================================

local menuToggleBtn = Instance.new("TextButton")
menuToggleBtn.Size = UDim2.new(0, 46, 0, 46)
menuToggleBtn.Position = UDim2.new(1, -66, 0, 16)
menuToggleBtn.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
menuToggleBtn.TextColor3 = parseColor(config.themeColorName)
menuToggleBtn.TextSize = 13
menuToggleBtn.Font = Enum.Font.SciFi
menuToggleBtn.Text = "SYS"
menuToggleBtn.Parent = screenGui

local menuToggleCorner = Instance.new("UICorner")
menuToggleCorner.CornerRadius = UDim.new(0, 6)
menuToggleCorner.Parent = menuToggleBtn

local menuToggleStroke = Instance.new("UIStroke")
menuToggleStroke.Color = parseColor(config.themeColorName)
menuToggleStroke.Thickness = 1.8
menuToggleStroke.Parent = menuToggleBtn

menuToggleBtn.MouseButton1Click:Connect(function()
    container.Visible = not container.Visible
end)

local camlockBtn = Instance.new("TextButton")
camlockBtn.Size = UDim2.new(0, 140, 0, 56)
camlockBtn.Position = UDim2.new(1, -156, 0.4, 0)
camlockBtn.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
camlockBtn.TextColor3 = parseColor(config.themeColorName)
camlockBtn.TextSize = 14
camlockBtn.Font = Enum.Font.SciFi
camlockBtn.Text = "CAMLOCK"
camlockBtn.Visible = false
camlockBtn.Parent = screenGui

local camlockCorner = Instance.new("UICorner")
camlockCorner.CornerRadius = UDim.new(0, 6)
camlockCorner.Parent = camlockBtn

local camlockBtnStroke = Instance.new("UIStroke")
camlockBtnStroke.Color = parseColor(config.themeColorName)
camlockBtnStroke.Thickness = 1.8
camlockBtnStroke.Parent = camlockBtn

local camlockDragging, camlockDragInput, camlockDragStart, camlockStartPos
camlockBtn.InputBegan:Connect(function(input)
    if config.camlockMovable and (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
        camlockDragging = true
        camlockDragStart = input.Position
        camlockStartPos = camlockBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                camlockDragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        camlockDragInput = input
    end
end)

RunService.RenderStepped:Connect(function()
    if config.camlockMovable and camlockDragging and camlockDragInput then
        local delta = camlockDragInput.Position - camlockDragStart
        camlockBtn.Position = UDim2.new(camlockStartPos.X.Scale, camlockStartPos.X.Offset + delta.X, camlockStartPos.Y.Scale, camlockStartPos.Y.Offset + delta.Y)
    end
end)

local infoFrame = Instance.new("Frame")
infoFrame.Size = UDim2.new(0, 180, 0, 52)
infoFrame.Position = UDim2.new(0.5, -90, 0, 16)
infoFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
infoFrame.BackgroundTransparency = 0.1
infoFrame.BorderSizePixel = 0
infoFrame.Visible = false
infoFrame.Parent = screenGui

local infoCorner = Instance.new("UICorner")
infoCorner.CornerRadius = UDim.new(0, 6)
infoCorner.Parent = infoFrame

local infoStroke = Instance.new("UIStroke")
infoStroke.Color = parseColor(config.themeColorName)
infoStroke.Thickness = 1.8
infoStroke.Parent = infoFrame

local targetAvatarImg = Instance.new("ImageLabel")
targetAvatarImg.Size = UDim2.new(0, 36, 0, 36)
targetAvatarImg.Position = UDim2.new(0, 8, 0.5, -18)
targetAvatarImg.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
targetAvatarImg.Parent = infoFrame

local avatarCorner = Instance.new("UICorner")
avatarCorner.CornerRadius = UDim.new(1, 0)
avatarCorner.Parent = targetAvatarImg

local targetNameLbl = Instance.new("TextLabel")
targetNameLbl.Size = UDim2.new(1, -52, 0, 16)
targetNameLbl.Position = UDim2.new(0, 50, 0, 8)
targetNameLbl.BackgroundTransparency = 1
targetNameLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
targetNameLbl.TextSize = 11
targetNameLbl.Font = Enum.Font.SciFi
targetNameLbl.TextXAlignment = Enum.TextXAlignment.Left
targetNameLbl.Parent = infoFrame

local targetDetailsLbl = Instance.new("TextLabel")
targetDetailsLbl.Size = UDim2.new(1, -52, 0, 16)
targetDetailsLbl.Position = UDim2.new(0, 50, 0, 26)
targetDetailsLbl.BackgroundTransparency = 1
targetDetailsLbl.TextColor3 = Color3.fromRGB(0, 255, 180)
targetDetailsLbl.TextSize = 9
targetDetailsLbl.Font = Enum.Font.Code
targetDetailsLbl.TextXAlignment = Enum.TextXAlignment.Left
targetDetailsLbl.Parent = infoFrame

-- ==================================================
-- RENDER & ESP LOOP
-- ==================================================

local espDrawings = {}

local function removeEspForPlayer(p)
    if espDrawings[p] then
        if espDrawings[p].box then pcall(function() espDrawings[p].box:Remove() end) end
        if espDrawings[p].name then pcall(function() espDrawings[p].name:Remove() end) end
        if espDrawings[p].details then pcall(function() espDrawings[p].details:Remove() end) end
        if espDrawings[p].trace then pcall(function() espDrawings[p].trace:Remove() end) end
        espDrawings[p] = nil
    end
end

Players.PlayerRemoving:Connect(function(p)
    removeEspForPlayer(p)
end)

local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = false
FOVCircle.Thickness = 1.5
FOVCircle.Color = parseColor(config.themeColorName)
FOVCircle.Filled = false

local function getClosestPlayer()
    local closest = nil
    local shortestDist = config.fovRadius
    local cam = Workspace.CurrentCamera
    local screenCenter = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player and p.Character and p.Character:FindFirstChild(config.targetPart) then
            local hum = p.Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                local targetPart = p.Character[config.targetPart]
                local screenPoint, onScreen = cam:WorldToViewportPoint(targetPart.Position)
                if onScreen then
                    local dist = (screenCenter - Vector2.new(screenPoint.X, screenPoint.Y)).Magnitude
                    if dist < shortestDist then
                        shortestDist = dist
                        closest = p
                    end
                end
            end
        end
    end
    return closest
end

local lockedPlayer = nil

local function toggleLock()
    if lockedPlayer then
        lockedPlayer = nil
        camlockBtn.TextColor3 = parseColor(config.themeColorName)
        infoFrame.Visible = false
    else
        lockedPlayer = getClosestPlayer()
        if lockedPlayer then
            camlockBtn.TextColor3 = Color3.fromRGB(0, 255, 180)
            targetNameLbl.Text = lockedPlayer.DisplayName
            targetAvatarImg.Image = "rbxthumb://type=AvatarHeadShot&id=" .. lockedPlayer.UserId .. "&w=150&h=150"
            infoFrame.Visible = true
        end
    end
end

camlockBtn.MouseButton1Click:Connect(toggleLock)

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == config.keybind then
        toggleLock()
    end
end)

RunService.RenderStepped:Connect(function()
    local cam = Workspace.CurrentCamera
    local viewportCenter = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)
    
    camlockBtn.Visible = config.enabled
    
    FOVCircle.Visible = config.showFov and config.enabled
    FOVCircle.Radius = config.fovRadius
    FOVCircle.Position = viewportCenter
    FOVCircle.Color = parseColor(config.themeColorName)
    
    menuToggleStroke.Color = parseColor(config.themeColorName)
    menuToggleBtn.TextColor3 = parseColor(config.themeColorName)
    camlockBtnStroke.Color = parseColor(config.themeColorName)
    infoStroke.Color = parseColor(config.themeColorName)
    
    if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if config.speedEnabled then
            hum.WalkSpeed = config.speedValue
        else
            hum.WalkSpeed = 16
        end
        
        if config.jumpEnabled then
            hum.JumpPower = config.jumpValue
            hum.UseJumpPower = true
        else
            hum.JumpPower = 50
        end
    end
    
    if config.enabled and lockedPlayer and lockedPlayer.Character then
        local targetPartObj = lockedPlayer.Character:FindFirstChild(config.targetPart)
        local hum = lockedPlayer.Character:FindFirstChildOfClass("Humanoid")
        
        if targetPartObj and hum and hum.Health > 0 then
            local targetPos = targetPartObj.Position + (targetPartObj.AssemblyLinearVelocity * config.prediction)
            cam.CFrame = cam.CFrame:Lerp(CFrame.new(cam.CFrame.Position, targetPos), config.smoothness)
            
            local distanceMeters = math.round((targetPartObj.Position - cam.CFrame.Position).Magnitude)
            local currentHealth = math.round(hum.Health)
            targetDetailsLbl.Text = "HP: " .. currentHealth .. " // DIST: " .. distanceMeters .. "M"
        else
            lockedPlayer = nil
            camlockBtn.TextColor3 = parseColor(config.themeColorName)
            infoFrame.Visible = false
        end
    else
        if not lockedPlayer then
            infoFrame.Visible = false
        end
    end
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            local char = p.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local rootPart = char and char:FindFirstChild("HumanoidRootPart")
            
            if config.espEnabled and char and hum and rootPart and hum.Health > 0 then
                local pos, onScreen = cam:WorldToViewportPoint(rootPart.Position)
                if onScreen then
                    if not espDrawings[p] then
                        espDrawings[p] = {
                            box = Drawing.new("Square"),
                            name = Drawing.new("Text"),
                            details = Drawing.new("Text"),
                            trace = Drawing.new("Line")
                        }
                        espDrawings[p].box.Filled = false
                        espDrawings[p].box.Thickness = 1.5
                        espDrawings[p].name.Size = 12
                        espDrawings[p].name.Center = true
                        espDrawings[p].name.Outline = true
                        espDrawings[p].details.Size = 10
                        espDrawings[p].details.Center = true
                        espDrawings[p].details.Outline = true
                        espDrawings[p].trace.Thickness = 1
                    end
                    
                    local data = espDrawings[p]
                    local dist = (rootPart.Position - cam.CFrame.Position).Magnitude
                    local sizeY = math.clamp(2500 / pos.Z, 15, 300)
                    local sizeX = sizeY * 0.65
                    
                    data.box.Visible = config.boxEnabled
                    data.box.Size = Vector2.new(sizeX, sizeY)
                    data.box.Position = Vector2.new(pos.X - sizeX / 2, pos.Y - sizeY / 2)
                    data.box.Color = parseColor(config.boxColorName)
                    
                    data.name.Visible = config.nameEnabled
                    data.name.Text = p.DisplayName
                    data.name.Position = Vector2.new(pos.X, pos.Y - sizeY / 2 - 16)
                    data.name.Color = parseColor(config.espColorName)
                    
                    local detailsText = ""
                    if config.healthEnabled and config.metersEnabled then
                        detailsText = "HP: " .. math.round(hum.Health) .. " | " .. math.round(dist) .. "M"
                    elseif config.healthEnabled then
                        detailsText = "HP: " .. math.round(hum.Health)
                    elseif config.metersEnabled then
                        detailsText = math.round(dist) .. "M"
                    end
                    
                    data.details.Visible = config.healthEnabled or config.metersEnabled
                    data.details.Text = detailsText
                    data.details.Position = Vector2.new(pos.X, pos.Y + sizeY / 2 + 4)
                    data.details.Color = parseColor(config.espColorName)
                    
                    data.trace.Visible = config.traceEnabled
                    data.trace.From = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y)
                    data.trace.To = Vector2.new(pos.X, pos.Y + sizeY / 2)
                    data.trace.Color = parseColor(config.traceColorName)
                else
                    removeEspForPlayer(p)
                end
            else
                removeEspForPlayer(p)
            end
            
            if config.chamsEnabled then
                local highlight = char and char:FindFirstChild("FoxxieChams")
                if lockedPlayer == p and char then
                    if not highlight then
                        highlight = Instance.new("Highlight")
                        highlight.Name = "FoxxieChams"
                        highlight.Adornee = char
                        highlight.FillColor = parseColor(config.themeColorName)
                        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                        highlight.FillTransparency = 0.25
                        highlight.Parent = char
                    end
                else
                    if highlight then highlight:Destroy() end
                end
            else
                if char then
                    local highlight = char:FindFirstChild("FoxxieChams")
                    if highlight then highlight:Destroy() end
                end
            end
        end
    end
end)

print("✅ FOXXIEHUB FUTURISTIC EDITION ONLINE.")